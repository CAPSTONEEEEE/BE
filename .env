# 실행 환경 (dev, prod 등)
ENVIRONMENT=dev

# 데이터베이스 연결 (SQLite 예시)
DATABASE_URL=sqlite:///./app.db

# 프론트엔드에서 접근할 수 있는 도메인 (CORS 허용)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# OpenAI API 키 (테스트 시 비워도 OK)
OPENAI_API_KEY=sk-xxxxxx
OPENAI_MODEL=gpt-4o-mini

